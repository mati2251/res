version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        emit_json_tags: true
        package: "db"
        out: "../pkg/db"
        sql_package: "pgx/v5"
